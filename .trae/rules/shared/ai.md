# 人工智能使用规范

# 生成md文件
- 除指定需要生成段落序号外，默认不生成段落序号 如1、1.1等

# 测试文件
- 在用户没有要求时，默认不生成单元测试文件
- 如需生成其他临时测试文件（非单元测试文件），需明确告诉用户，并在用户确认后才能够生成。且生成的临时测试文件统一放到一个名为`temp_test`的目录下，该目录随时可以被清理，因此不要放入重要的文件到该目录下

# pom生成
- 所有的版本依赖均使用变量引用，版本定义从上下文的<properties>标签中读取
- pom生成使用的groupId、artifactId均从`variables.md`中读取
